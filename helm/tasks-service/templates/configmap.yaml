apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-tasks-service-config
  labels:
    app: {{ .Release.Name }}-tasks-service
data:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
