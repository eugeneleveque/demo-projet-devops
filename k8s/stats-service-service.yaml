apiVersion: v1
kind: Service
metadata:
  name: stats-service
  labels:
    app: stats-service
spec:
  type: NodePort
  selector:
    app: stats-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002
      nodePort: 30002
